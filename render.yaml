services:
  - type: web
    name: erebus-api
    runtime: node
    buildCommand: npm install && npx expo export --platform web --output-dir dist --clear && cd server && npm install
    startCommand: node server/index.js
    healthCheckPath: /api/health
