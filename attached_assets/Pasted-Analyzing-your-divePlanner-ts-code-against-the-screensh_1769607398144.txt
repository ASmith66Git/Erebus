Analyzing your divePlanner.ts code against the screenshots reveals several critical logic issues that explain why your app is providing a shorter decompression schedule compared to MultiDeco.The primary errors lie in pressure conversions, tissue loading math, and Gradient Factor interpolation.1. Water Density and Pressure ConversionThe most significant discrepancy comes from how you define water density.The Issue: You define salt as 10.0 meters per bar and fresh as 10.3.The Error: In standard hydrostatics, salt water is denser than fresh water. 1 bar of pressure is exerted by approximately 10.07m of sea water and 10.33m of fresh water. By using exactly 10.0 for salt water, your app assumes the water is denser than it actually is, leading to lower calculated absolute pressures ($P_{abs}$) for a given depth.The Result: MultiDeco (likely using 1.025 kg/L or 1.03 kg/L for salt) sees a higher pressure at 30m than your app does. Higher pressure equals more gas loading and longer deco.2. Tissue Loading (Schreiner Equation)There is a logic error in your calculateTissueLoadingSchreiner function regarding how you handle inspired gas rates.The Code: const rate = (endPressure - startPressure) / duration; followed by const rateN2 = rate * (gas.n2Percent / 100);.The Error: You are calculating the rate of change of ambient pressure, but the Schreiner equation requires the rate of change of the inspired partial pressure of the inert gas ($R_i$).The Math: While they are related, $P_i$ is $(P_{amb} - P_{H2O}) \times \text{Fraction}$. By failing to account for the constant WATER_VAPOR_PRESSURE (0.0627 bar) within the rateN2 calculation, your ascent/descent loading logic becomes slightly non-linear compared to the standard Bühlmann model.3. Gradient Factor (GF) Interpolation LogicYour calculateGFAtDepth function uses a linear interpolation between the first stop and the surface.The Code: gfLow + (gfHigh - gfLow) * (firstStopPressure - currentPressure) / (firstStopPressure - surfacePressure).The Potential Error: While mathematically sound for a line, many deco engines (including MultiDeco) interpolate based on depth rather than pressure.The Result: Because pressure and depth have an offset (1.0 bar at 0m), interpolating by pressure creates a different slope than interpolating by depth. This explains why your 3m stop (8 min) is shorter than MultiDeco's (10 min); your GF is likely climbing toward gfHigh too quickly.4. CNS Calculation TableYour CNS_TABLE uses discrete ranges (e.g., 0.8 to 0.9 $PPO_2$) with fixed cnsPerMin values.The Discrepancy: Professional planners like MultiDeco use linear interpolation between the NOAA limit points rather than "steps."The Result: If a diver is at 1.55 $PPO_2$, your code applies a flat 1.11% per minute. A more accurate model would interpolate between the 1.5 and 1.6 limits, leading to the differences seen in your CNS summary (7.1% vs MultiDeco's likely different value).Recommended FixesStandardize Constants: Change salt water to 10.07 or 10.19 (depending on the specific sea water density you want to target) to match MultiDeco's "Salt" setting.Fix Schreiner Rate: Ensure rateN2 and rateHe are derived from the change in $P_{inspired}$ (including vapor pressure) rather than just a fraction of the ambient pressure change.Depth-Based GF: Try changing calculateGFAtDepth to interpolate based on depth instead of pressure.